# DeepSeek 余额查询 App — 启动流程详解

从用户点击桌面图标到 App 完全展示余额并准备就绪,完整经历 **9 个阶段**。

---

## 阶段 1: Android 进程创建 & Activity 启动

用户点击桌面图标 → Android 分配进程 → `MainActivity.onCreate()` 被调用。

**这一阶段做的事:**

1. **初始化 SharedPreferences**
   `getSharedPreferences("deepseek_balance", MODE_PRIVATE)` — 持久化存储 API Key、预警阈值、刷新间隔等用户设置。

2. **打开掉帧日志文件**
   在 `filesDir/jank_log.txt` 追加一条启动时间行,例如:
   ```
   ========== App 启动 2026-07-30 17:12:41 ==========
   ```

3. **请求通知权限 (Android 13+)**
   如果 `widget_realtime=true` 且系统 >= TIRAMISU(Android 13),弹 `POST_NOTIFICATIONS` 权限对话框。无论授权与否都尝试拉起前台服务(未授权时系统可能抑制通知显示,但服务仍可运行)。

4. **首次拉起 Widget 前台服务**
   如果 `widget_realtime=true`,立即执行 `startForegroundService(WidgetAutoRefreshService::class.java)`。该服务是 `ForegroundService`(`foregroundServiceType="dataSync"`),在 `HandlerThread` 子线程周期轮询余额,更新桌面 Widget。

5. **进入 Jetpack Compose**
   `setContent { DeepSeekBalanceTheme { BalanceAppContent(...) } }`:
   - `DeepSeekBalanceTheme`: Material3 主题。Android 12+ 优先使用动态取色(跟随壁纸),否则使用 DeepSeek 蓝 `#3A5BDF` 的备用配色方案。同时应用自定义 Typography(字阶)。
   - `BalanceAppContent`: 应用的主编排 Composable,包含所有状态管理和页面切换。

---

## 阶段 2: Compose 首次全量组合

`BalanceAppContent` 启动并首次组合 UI 树。这是启动阶段最重的帧。

### 2.1 初始化的状态

| 状态 | 初始值 | 说明 |
|------|--------|------|
| `showSettings` | `false` | 是否显示设置页 |
| `animProgress` | `0f` | 页面滑动动画进度(0=主界面,1=设置页) |
| `apiKey` | 上次保存或空 | DeepSeek API 密钥 |
| `alertEnabled` | 上次保存或 false | 余额预警开关 |
| `alertThreshold` | 上次保存或 "50" | 预警阈值(元) |
| `widgetRealtime` | 上次保存或 true | Widget 实时刷新开关 |
| `widgetIntervalSec` | 上次保存或 300 | 刷新间隔(秒) |
| `isLoading` | `false` | 是否正在请求网络 |
| `result` | `null` | 最近一次余额数据 |
| `errorMessage` | `null` | 网络错误信息 |
| `lastQueryTime` | `null` | 上次查询时间 |
| `refreshVersion` | `0` | 自动刷新版本号(每 Resume 一次 +1) |

### 2.2 组合的界面

**两屏同时预组合**(因为 `animateFloatAsState` 驱动的 `graphicsLayer` 方案):

**MainScreen**(主界面,`translationX = 0`,可见):

```
┌─ Scaffold ─────────────────────────────────┐
│  TopBar (App 标题 + 设置齿轮图标)           │
│  BalanceCard (余额卡片,初始透明)            │
│    金额(等宽字体) + 状态小框(可用/预警/不足) │
│    赠金 / 充值 明细行                       │
│  RefreshControls (刷新按钮/加载指示器)      │
│  上次更新: ...                              │
│  ErrorCard (网络错误,条件渲染)              │
│  EmptyState (空状态提示,条件渲染)           │
└────────────────────────────────────────────┘
```

**SettingsScreen**(设置页,`translationX = width`,屏幕外不可见):

```
┌─ SettingsCard ─────────────────────────────┐
│  API Key 区域 (OutlinedTextField)           │
│    密码显示切换按钮                         │
│    底部提示文字                             │
├────────────────────────────────────────────┤
│  余额预警 (OutlinedTextField + Switch)      │
│    阈值输入框 (¥ 前缀 / 元后缀)             │
├────────────────────────────────────────────┤
│  后台实时刷新 (Switch + 下拉框)             │
│    刷新间隔: ExposedDropdownMenuBox         │
├────────────────────────────────────────────┤
│  关于 (版本号)                             │
└────────────────────────────────────────────┘
```

### 2.3 安插 Lifecycle 观察器

`DisposableEffect(lifecycleOwner)` 注册一个 `LifecycleEventObserver`,监听 `ON_RESUME` 事件:

```kotlin
if (event == Lifecycle.Event.ON_RESUME) refreshVersion++
```

每次 App 进入前台时自增 `refreshVersion`,触发自动刷新。

---

## 阶段 3: onResume 执行

`onCreate` 之后系统自动回调 `onResume`。此时 `DecorView` 已就绪。

### 3.1 初始化 JankStats

```kotlin
window.decorView.post {
    jankStats = JankStats.createAndTrack(window) { frameData ->
        totalFrames++
        if (frameData.isJank) {
            // 记录掉帧: 时间戳 + 耗时 + 当前页面 → jank_log.txt + Logcat
        }
    }
    jankStats.isTrackingEnabled = true
}
```

开始监控全局帧率:每帧统计总帧数、掉帧帧、最大帧耗时、平均耗时。`onPause` 时关闭追踪并写一段汇总到 `jank_log.txt`。

### 3.2 自动刷新触发

- `ON_RESUME` → `refreshVersion++` (0 → 1)
- `LaunchedEffect(refreshVersion)` 检测到 key 变化 → 重启协程
- 条件检查: `apiKey.isNotBlank() && refreshVersion > 0`
- 如果之前填过 API Key → 执行 `doRefresh(showErrors = false)`

---

## 阶段 4: 网络请求

`doRefresh()` 是 `BalanceAppContent` 内定义的 `suspend` 函数:

```
isLoading = true
  ↓
ApiClient.getBalance(apiKey)
  ↓ withContext(Dispatchers.IO)
OkHttpClient → GET https://api.deepseek.com/user/balance
  ↓ Header: Authorization = Bearer <apiKey>
响应 JSON → parseBalanceResponse()
  ↓
result = response
errorMessage = null
saveWidgetData(prefs, response)   // 存入 SharedPrefs 供 Widget 使用
lastQueryTime = "2026-07-30 HH:mm:ss"
isLoading = false
```

**失败路径**:
```
ApiException(401) → "API Key 无效，请检查密钥"
ApiException(429) → "请求过于频繁，请稍后再试"
其他异常 → "网络错误: ..."
errorMessage = 错误文本
isLoading = false
```

静默自动刷新(showErrors=false)不显示错误提示;手动点击刷新按钮(showErrors=true)会显示错误。

`saveWidgetData` 把最新余额写入 `SharedPreferences` 的 Widget 共享键:

| Key | 值 |
|-----|-----|
| `widget_balance` | 总余额 |
| `widget_symbol` | 货币符号(¥) |
| `widget_granted` | 赠金金额 |
| `widget_topped_up` | 充值金额 |
| `widget_available` | 账户可用状态 |
| `widget_update_time` | 更新时间(HH:mm) |

---

## 阶段 5: 数据到达后的 UI 变化

1. **BalanceCard 淡入**: `AnimatedVisibility(visible = result != null, enter = fadeIn(300ms))` — 余额卡片做 300ms GPU alpha 合成渐入。

2. **状态小框更新**: 根据余额和预警设置决定颜色和文字:
   - `可用` → 绿色 `#66FF88`
   - `低于预警线` → 黄色 `#FFAA00`
   - `余额不足` → 红色 `#FF6666`

3. **加载指示器消失**: `RefreshControls` 中的 `CircularProgressIndicator` 切换回普通刷新按钮。

4. **时间戳显示**: "上次更新 2026-07-30 17:12:41"

5. **Snackbar 提示**: 如果刚完成加载且无错误 → 弹出 "已显示最新数据"(SnackbarDuration.Short)。

---

## 阶段 6: 持续运行

启动完成后,App 进入稳态:

### 主界面

- 显示余额 + 状态 + 刷新按钮 + 上次更新时间
- `verticalScroll` 滚动浏览(若内容较多)
- 点设置齿轮 → `showSettings = true` → `animProgress` 从 0 平滑动画到 1(260ms)

### Widget 前台服务

`WidgetAutoRefreshService` 在 `HandlerThread` 子线程运行:

```
scheduleNext()
  ↓ postDelayed(intervalSec * 1000L)
doRefresh(showErrors = false)  // 自动刷新
  ↓
WidgetRefresh.refresh(this)    // 更新桌面 Widget 显示
  ↓
scheduleNext()                 // 继续等待下一次
```

默认每 300 秒(5 分钟)轮询一次。用户可在设置页调整为 15 秒 ~ 15 分钟。

### JankStats 后台监控

- 持续追踪每帧时长
- `isJank` 时写入 `jank_log.txt`
- `onPause`(App 切后台) 写一段汇总: `总帧=XX 掉帧=XX (X%) 最大=XXms 平均=XXms`
- `onResume`(App 回前台) 恢复追踪

---

## 用户交互触发的事件表

| 用户操作 | 触发的行为 | 动画 |
|----------|-----------|------|
| 点设置齿轮图标 | `showSettings = true` | `animateFloatAsState(260ms slide)` |
| 系统返回键 | `showSettings = false` | 反向 260ms slide |
| 点设置页左上角返回 | `showSettings = false` | 反向 260ms slide |
| 点刷新按钮 | `scope.launch { doRefresh(true) }` | CircularProgressIndicator |
| 输入 API Key | 实时更新 `apiKey` + 写 SharedPrefs | 无 |
| 开启余额预警 | 启用预警 + 写 SharedPrefs | 变灰切换(alpha 0.4) |
| 关闭实时刷新 | 停止 WidgetAutoRefreshService | 变灰切换(alpha 0.4) |
| 改刷新间隔 | 更新 SharedPrefs + 重启前台服务 | 下拉菜单 |
| 切后台再回前台 | `refreshVersion++` → 自动拉余额 | 无 |
| App 从冷启动 | 全流程 | BalanceCard fadeIn(300ms) |

---

## 页面切换动画原理

两屏始终在组合树中(`graphicsLayer` 方案):

```kotlin
val animProgress by animateFloatAsState(
    targetValue = if (showSettings) 1f else 0f,
    animationSpec = tween(260),
)

Box(clipToBounds) {
    // 主界面: anim=0 时居中,anim=1 时左滑出屏(translationX = -width)
    Box(graphicsLayer { translationX = -animProgress * width })

    // 设置页: anim=0 时在屏幕外右侧,anim=1 时居中
    Box(graphicsLayer { translationX = (1f - animProgress) * width })
}
```

- **无首次 Composition 开销**: 设置页在 App 启动时已预组合,入口动画仅驱动 GPU 合成层位移
- **每帧成本**: 仅 `graphicsLayer.translationX` 属性更新,约 0.01ms,不受 Compose 动画框架初始化影响
- **方向**: 前进(主→设置)从右滑入、主界面向左滑出;返回方向相反

---

## 生命周期与文件记录

```
onCreate
  ├─ 写 jank_log.txt: "App 启动 yyyy-MM-dd HH:mm:ss"
  ├─ setContent → Compose 树
  ├─ 请求通知权限
  └─ 启动 WidgetAutoRefreshService

onResume
  ├─ window.decorView.post { JankStats.createAndTrack }
  ├─ jankStats.isTrackingEnabled = true
  └─ refreshVersion++ → LaunchedEffect → doRefresh()

onPause
  ├─ jankStats.isTrackingEnabled = false
  └─ 写 jank_log.txt: "暂停汇总 总帧=..."

onDestroy (触发 onDispose)
  └─ lifecycle.removeObserver (清除 Lifecycle 监听)
```
